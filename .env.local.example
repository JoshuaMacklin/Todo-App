# Convex (run `npx convex dev` to populate these)
CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_URL=

# Convex Auth (set in Convex dashboard or via `npx convex env set`)
# SITE_URL - e.g. http://localhost:3000
# JWT_PRIVATE_KEY, JWKS - run generateKeys.mjs from Convex Auth manual setup
# AUTH_GITHUB_ID, AUTH_GITHUB_SECRET - from GitHub OAuth App
# AUTH_GOOGLE_ID, AUTH_GOOGLE_SECRET - from Google Cloud OAuth client
